import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.financiero.mx.app',
  appName: 'Financiero',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
